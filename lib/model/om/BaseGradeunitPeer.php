<?php

/**
 * Base static class for performing query and update operations on the 'GradeUnit' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * Seg 07 Mar 2011 16:26:13 BRT
 *
 * @package    lib.model.om
 */
abstract class BaseGradeunitPeer {

	/** the default database name for this class */
	const DATABASE_NAME = 'propel';

	/** the table name for this class */
	const TABLE_NAME = 'GradeUnit';

	/** the related Propel class for this table */
	const OM_CLASS = 'Gradeunit';

	/** A class that can be returned by this peer. */
	const CLASS_DEFAULT = 'lib.model.Gradeunit';

	/** the related TableMap class for this table */
	const TM_CLASS = 'GradeunitTableMap';
	
	/** The total number of columns. */
	const NUM_COLUMNS = 7;

	/** The number of lazy-loaded columns. */
	const NUM_LAZY_LOAD_COLUMNS = 0;

	/** the column name for the ID field */
	const ID = 'GradeUnit.ID';

	/** the column name for the HORARIO_ID field */
	const HORARIO_ID = 'GradeUnit.HORARIO_ID';

	/** the column name for the DISCIPLINA_ID field */
	const DISCIPLINA_ID = 'GradeUnit.DISCIPLINA_ID';

	/** the column name for the PROFESSOR_ID field */
	const PROFESSOR_ID = 'GradeUnit.PROFESSOR_ID';

	/** the column name for the PERIODO_ID field */
	const PERIODO_ID = 'GradeUnit.PERIODO_ID';

	/** the column name for the LOCAL_ID field */
	const LOCAL_ID = 'GradeUnit.LOCAL_ID';

	/** the column name for the WEEKDAY_ID field */
	const WEEKDAY_ID = 'GradeUnit.WEEKDAY_ID';

	/**
	 * An identiy map to hold any loaded instances of Gradeunit objects.
	 * This must be public so that other peer classes can access this when hydrating from JOIN
	 * queries.
	 * @var        array Gradeunit[]
	 */
	public static $instances = array();


	// symfony behavior
	
	/**
	 * Indicates whether the current model includes I18N.
	 */
	const IS_I18N = false;

	/**
	 * holds an array of fieldnames
	 *
	 * first dimension keys are the type constants
	 * e.g. self::$fieldNames[self::TYPE_PHPNAME][0] = 'Id'
	 */
	private static $fieldNames = array (
		BasePeer::TYPE_PHPNAME => array ('Id', 'HorarioId', 'DisciplinaId', 'ProfessorId', 'PeriodoId', 'LocalId', 'WeekdayId', ),
		BasePeer::TYPE_STUDLYPHPNAME => array ('id', 'horarioId', 'disciplinaId', 'professorId', 'periodoId', 'localId', 'weekdayId', ),
		BasePeer::TYPE_COLNAME => array (self::ID, self::HORARIO_ID, self::DISCIPLINA_ID, self::PROFESSOR_ID, self::PERIODO_ID, self::LOCAL_ID, self::WEEKDAY_ID, ),
		BasePeer::TYPE_FIELDNAME => array ('id', 'horario_id', 'disciplina_id', 'professor_id', 'periodo_id', 'local_id', 'weekDay_id', ),
		BasePeer::TYPE_NUM => array (0, 1, 2, 3, 4, 5, 6, )
	);

	/**
	 * holds an array of keys for quick access to the fieldnames array
	 *
	 * first dimension keys are the type constants
	 * e.g. self::$fieldNames[BasePeer::TYPE_PHPNAME]['Id'] = 0
	 */
	private static $fieldKeys = array (
		BasePeer::TYPE_PHPNAME => array ('Id' => 0, 'HorarioId' => 1, 'DisciplinaId' => 2, 'ProfessorId' => 3, 'PeriodoId' => 4, 'LocalId' => 5, 'WeekdayId' => 6, ),
		BasePeer::TYPE_STUDLYPHPNAME => array ('id' => 0, 'horarioId' => 1, 'disciplinaId' => 2, 'professorId' => 3, 'periodoId' => 4, 'localId' => 5, 'weekdayId' => 6, ),
		BasePeer::TYPE_COLNAME => array (self::ID => 0, self::HORARIO_ID => 1, self::DISCIPLINA_ID => 2, self::PROFESSOR_ID => 3, self::PERIODO_ID => 4, self::LOCAL_ID => 5, self::WEEKDAY_ID => 6, ),
		BasePeer::TYPE_FIELDNAME => array ('id' => 0, 'horario_id' => 1, 'disciplina_id' => 2, 'professor_id' => 3, 'periodo_id' => 4, 'local_id' => 5, 'weekDay_id' => 6, ),
		BasePeer::TYPE_NUM => array (0, 1, 2, 3, 4, 5, 6, )
	);

	/**
	 * Translates a fieldname to another type
	 *
	 * @param      string $name field name
	 * @param      string $fromType One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                         BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @param      string $toType   One of the class type constants
	 * @return     string translated name of the field.
	 * @throws     PropelException - if the specified name could not be found in the fieldname mappings.
	 */
	static public function translateFieldName($name, $fromType, $toType)
	{
		$toNames = self::getFieldNames($toType);
		$key = isset(self::$fieldKeys[$fromType][$name]) ? self::$fieldKeys[$fromType][$name] : null;
		if ($key === null) {
			throw new PropelException("'$name' could not be found in the field names of type '$fromType'. These are: " . print_r(self::$fieldKeys[$fromType], true));
		}
		return $toNames[$key];
	}

	/**
	 * Returns an array of field names.
	 *
	 * @param      string $type The type of fieldnames to return:
	 *                      One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                      BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     array A list of field names
	 */

	static public function getFieldNames($type = BasePeer::TYPE_PHPNAME)
	{
		if (!array_key_exists($type, self::$fieldNames)) {
			throw new PropelException('Method getFieldNames() expects the parameter $type to be one of the class constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME, BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. ' . $type . ' was given.');
		}
		return self::$fieldNames[$type];
	}

	/**
	 * Convenience method which changes table.column to alias.column.
	 *
	 * Using this method you can maintain SQL abstraction while using column aliases.
	 * <code>
	 *		$c->addAlias("alias1", TablePeer::TABLE_NAME);
	 *		$c->addJoin(TablePeer::alias("alias1", TablePeer::PRIMARY_KEY_COLUMN), TablePeer::PRIMARY_KEY_COLUMN);
	 * </code>
	 * @param      string $alias The alias for the current table.
	 * @param      string $column The column name for current table. (i.e. GradeunitPeer::COLUMN_NAME).
	 * @return     string
	 */
	public static function alias($alias, $column)
	{
		return str_replace(GradeunitPeer::TABLE_NAME.'.', $alias.'.', $column);
	}

	/**
	 * Add all the columns needed to create a new object.
	 *
	 * Note: any columns that were marked with lazyLoad="true" in the
	 * XML schema will not be added to the select list and only loaded
	 * on demand.
	 *
	 * @param      criteria object containing the columns to add.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function addSelectColumns(Criteria $criteria)
	{
		$criteria->addSelectColumn(GradeunitPeer::ID);
		$criteria->addSelectColumn(GradeunitPeer::HORARIO_ID);
		$criteria->addSelectColumn(GradeunitPeer::DISCIPLINA_ID);
		$criteria->addSelectColumn(GradeunitPeer::PROFESSOR_ID);
		$criteria->addSelectColumn(GradeunitPeer::PERIODO_ID);
		$criteria->addSelectColumn(GradeunitPeer::LOCAL_ID);
		$criteria->addSelectColumn(GradeunitPeer::WEEKDAY_ID);
	}

	/**
	 * Returns the number of rows matching criteria.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @return     int Number of matching rows.
	 */
	public static function doCount(Criteria $criteria, $distinct = false, PropelPDO $con = null)
	{
		// we may modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(GradeunitPeer::TABLE_NAME);

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			GradeunitPeer::addSelectColumns($criteria);
		}

		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count
		$criteria->setDbName(self::DATABASE_NAME); // Set the correct dbName

		if ($con === null) {
			$con = Propel::getConnection(GradeunitPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}
		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}

		// BasePeer returns a PDOStatement
		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}
	/**
	 * Method to select one object from the DB.
	 *
	 * @param      Criteria $criteria object used to create the SELECT statement.
	 * @param      PropelPDO $con
	 * @return     Gradeunit
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectOne(Criteria $criteria, PropelPDO $con = null)
	{
		$critcopy = clone $criteria;
		$critcopy->setLimit(1);
		$objects = GradeunitPeer::doSelect($critcopy, $con);
		if ($objects) {
			return $objects[0];
		}
		return null;
	}
	/**
	 * Method to do selects.
	 *
	 * @param      Criteria $criteria The Criteria object used to build the SELECT statement.
	 * @param      PropelPDO $con
	 * @return     array Array of selected Objects
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelect(Criteria $criteria, PropelPDO $con = null)
	{
		return GradeunitPeer::populateObjects(GradeunitPeer::doSelectStmt($criteria, $con));
	}
	/**
	 * Prepares the Criteria object and uses the parent doSelect() method to execute a PDOStatement.
	 *
	 * Use this method directly if you want to work with an executed statement durirectly (for example
	 * to perform your own object hydration).
	 *
	 * @param      Criteria $criteria The Criteria object used to build the SELECT statement.
	 * @param      PropelPDO $con The connection to use
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 * @return     PDOStatement The executed PDOStatement object.
	 * @see        BasePeer::doSelect()
	 */
	public static function doSelectStmt(Criteria $criteria, PropelPDO $con = null)
	{
		if ($con === null) {
			$con = Propel::getConnection(GradeunitPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		if (!$criteria->hasSelectClause()) {
			$criteria = clone $criteria;
			GradeunitPeer::addSelectColumns($criteria);
		}

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);
		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}


		// BasePeer returns a PDOStatement
		return BasePeer::doSelect($criteria, $con);
	}
	/**
	 * Adds an object to the instance pool.
	 *
	 * Propel keeps cached copies of objects in an instance pool when they are retrieved
	 * from the database.  In some cases -- especially when you override doSelect*()
	 * methods in your stub classes -- you may need to explicitly add objects
	 * to the cache in order to ensure that the same objects are always returned by doSelect*()
	 * and retrieveByPK*() calls.
	 *
	 * @param      Gradeunit $value A Gradeunit object.
	 * @param      string $key (optional) key to use for instance map (for performance boost if key was already calculated externally).
	 */
	public static function addInstanceToPool(Gradeunit $obj, $key = null)
	{
		if (Propel::isInstancePoolingEnabled()) {
			if ($key === null) {
				$key = (string) $obj->getId();
			} // if key === null
			self::$instances[$key] = $obj;
		}
	}

	/**
	 * Removes an object from the instance pool.
	 *
	 * Propel keeps cached copies of objects in an instance pool when they are retrieved
	 * from the database.  In some cases -- especially when you override doDelete
	 * methods in your stub classes -- you may need to explicitly remove objects
	 * from the cache in order to prevent returning objects that no longer exist.
	 *
	 * @param      mixed $value A Gradeunit object or a primary key value.
	 */
	public static function removeInstanceFromPool($value)
	{
		if (Propel::isInstancePoolingEnabled() && $value !== null) {
			if (is_object($value) && $value instanceof Gradeunit) {
				$key = (string) $value->getId();
			} elseif (is_scalar($value)) {
				// assume we've been passed a primary key
				$key = (string) $value;
			} else {
				$e = new PropelException("Invalid value passed to removeInstanceFromPool().  Expected primary key or Gradeunit object; got " . (is_object($value) ? get_class($value) . ' object.' : var_export($value,true)));
				throw $e;
			}

			unset(self::$instances[$key]);
		}
	} // removeInstanceFromPool()

	/**
	 * Retrieves a string version of the primary key from the DB resultset row that can be used to uniquely identify a row in this table.
	 *
	 * For tables with a single-column primary key, that simple pkey value will be returned.  For tables with
	 * a multi-column primary key, a serialize()d version of the primary key will be returned.
	 *
	 * @param      string $key The key (@see getPrimaryKeyHash()) for this instance.
	 * @return     Gradeunit Found object or NULL if 1) no instance exists for specified key or 2) instance pooling has been disabled.
	 * @see        getPrimaryKeyHash()
	 */
	public static function getInstanceFromPool($key)
	{
		if (Propel::isInstancePoolingEnabled()) {
			if (isset(self::$instances[$key])) {
				return self::$instances[$key];
			}
		}
		return null; // just to be explicit
	}
	
	/**
	 * Clear the instance pool.
	 *
	 * @return     void
	 */
	public static function clearInstancePool()
	{
		self::$instances = array();
	}
	
	/**
	 * Method to invalidate the instance pool of all tables related to GradeUnit
	 * by a foreign key with ON DELETE CASCADE
	 */
	public static function clearRelatedInstancePool()
	{
	}

	/**
	 * Retrieves a string version of the primary key from the DB resultset row that can be used to uniquely identify a row in this table.
	 *
	 * For tables with a single-column primary key, that simple pkey value will be returned.  For tables with
	 * a multi-column primary key, a serialize()d version of the primary key will be returned.
	 *
	 * @param      array $row PropelPDO resultset row.
	 * @param      int $startcol The 0-based offset for reading from the resultset row.
	 * @return     string A string version of PK or NULL if the components of primary key in result array are all null.
	 */
	public static function getPrimaryKeyHashFromRow($row, $startcol = 0)
	{
		// If the PK cannot be derived from the row, return NULL.
		if ($row[$startcol] === null) {
			return null;
		}
		return (string) $row[$startcol];
	}

	/**
	 * The returned array will contain objects of the default type or
	 * objects that inherit from the default.
	 *
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function populateObjects(PDOStatement $stmt)
	{
		$results = array();
	
		// set the class once to avoid overhead in the loop
		$cls = GradeunitPeer::getOMClass(false);
		// populate the object(s)
		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key = GradeunitPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj = GradeunitPeer::getInstanceFromPool($key))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj->hydrate($row, 0, true); // rehydrate
				$results[] = $obj;
			} else {
				$obj = new $cls();
				$obj->hydrate($row);
				$results[] = $obj;
				GradeunitPeer::addInstanceToPool($obj, $key);
			} // if key exists
		}
		$stmt->closeCursor();
		return $results;
	}

	/**
	 * Returns the number of rows matching criteria, joining the related Horario table
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinHorario(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(GradeunitPeer::TABLE_NAME);

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			GradeunitPeer::addSelectColumns($criteria);
		}
		
		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count
		
		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(GradeunitPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$criteria->addJoin(GradeunitPeer::HORARIO_ID, HorarioPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}

		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related Disciplina table
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinDisciplina(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(GradeunitPeer::TABLE_NAME);

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			GradeunitPeer::addSelectColumns($criteria);
		}
		
		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count
		
		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(GradeunitPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$criteria->addJoin(GradeunitPeer::DISCIPLINA_ID, DisciplinaPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}

		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related Professor table
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinProfessor(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(GradeunitPeer::TABLE_NAME);

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			GradeunitPeer::addSelectColumns($criteria);
		}
		
		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count
		
		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(GradeunitPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$criteria->addJoin(GradeunitPeer::PROFESSOR_ID, ProfessorPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}

		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related Periodo table
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinPeriodo(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(GradeunitPeer::TABLE_NAME);

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			GradeunitPeer::addSelectColumns($criteria);
		}
		
		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count
		
		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(GradeunitPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$criteria->addJoin(GradeunitPeer::PERIODO_ID, PeriodoPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}

		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related Local table
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinLocal(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(GradeunitPeer::TABLE_NAME);

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			GradeunitPeer::addSelectColumns($criteria);
		}
		
		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count
		
		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(GradeunitPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$criteria->addJoin(GradeunitPeer::LOCAL_ID, LocalPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}

		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related Week table
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinWeek(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(GradeunitPeer::TABLE_NAME);

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			GradeunitPeer::addSelectColumns($criteria);
		}
		
		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count
		
		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(GradeunitPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$criteria->addJoin(GradeunitPeer::WEEKDAY_ID, WeekPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}

		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Selects a collection of Gradeunit objects pre-filled with their Horario objects.
	 * @param      Criteria  $criteria
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of Gradeunit objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinHorario(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$criteria = clone $criteria;

		// Set the correct dbName if it has not been overridden
		if ($criteria->getDbName() == Propel::getDefaultDB()) {
			$criteria->setDbName(self::DATABASE_NAME);
		}

		GradeunitPeer::addSelectColumns($criteria);
		$startcol = (GradeunitPeer::NUM_COLUMNS - GradeunitPeer::NUM_LAZY_LOAD_COLUMNS);
		HorarioPeer::addSelectColumns($criteria);

		$criteria->addJoin(GradeunitPeer::HORARIO_ID, HorarioPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}

		$stmt = BasePeer::doSelect($criteria, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = GradeunitPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = GradeunitPeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {

				$cls = GradeunitPeer::getOMClass(false);

				$obj1 = new $cls();
				$obj1->hydrate($row);
				GradeunitPeer::addInstanceToPool($obj1, $key1);
			} // if $obj1 already loaded

			$key2 = HorarioPeer::getPrimaryKeyHashFromRow($row, $startcol);
			if ($key2 !== null) {
				$obj2 = HorarioPeer::getInstanceFromPool($key2);
				if (!$obj2) {

					$cls = HorarioPeer::getOMClass(false);

					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol);
					HorarioPeer::addInstanceToPool($obj2, $key2);
				} // if obj2 already loaded
				
				// Add the $obj1 (Gradeunit) to $obj2 (Horario)
				$obj2->addGradeunit($obj1);

			} // if joined row was not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of Gradeunit objects pre-filled with their Disciplina objects.
	 * @param      Criteria  $criteria
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of Gradeunit objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinDisciplina(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$criteria = clone $criteria;

		// Set the correct dbName if it has not been overridden
		if ($criteria->getDbName() == Propel::getDefaultDB()) {
			$criteria->setDbName(self::DATABASE_NAME);
		}

		GradeunitPeer::addSelectColumns($criteria);
		$startcol = (GradeunitPeer::NUM_COLUMNS - GradeunitPeer::NUM_LAZY_LOAD_COLUMNS);
		DisciplinaPeer::addSelectColumns($criteria);

		$criteria->addJoin(GradeunitPeer::DISCIPLINA_ID, DisciplinaPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}

		$stmt = BasePeer::doSelect($criteria, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = GradeunitPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = GradeunitPeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {

				$cls = GradeunitPeer::getOMClass(false);

				$obj1 = new $cls();
				$obj1->hydrate($row);
				GradeunitPeer::addInstanceToPool($obj1, $key1);
			} // if $obj1 already loaded

			$key2 = DisciplinaPeer::getPrimaryKeyHashFromRow($row, $startcol);
			if ($key2 !== null) {
				$obj2 = DisciplinaPeer::getInstanceFromPool($key2);
				if (!$obj2) {

					$cls = DisciplinaPeer::getOMClass(false);

					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol);
					DisciplinaPeer::addInstanceToPool($obj2, $key2);
				} // if obj2 already loaded
				
				// Add the $obj1 (Gradeunit) to $obj2 (Disciplina)
				$obj2->addGradeunit($obj1);

			} // if joined row was not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of Gradeunit objects pre-filled with their Professor objects.
	 * @param      Criteria  $criteria
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of Gradeunit objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinProfessor(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$criteria = clone $criteria;

		// Set the correct dbName if it has not been overridden
		if ($criteria->getDbName() == Propel::getDefaultDB()) {
			$criteria->setDbName(self::DATABASE_NAME);
		}

		GradeunitPeer::addSelectColumns($criteria);
		$startcol = (GradeunitPeer::NUM_COLUMNS - GradeunitPeer::NUM_LAZY_LOAD_COLUMNS);
		ProfessorPeer::addSelectColumns($criteria);

		$criteria->addJoin(GradeunitPeer::PROFESSOR_ID, ProfessorPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}

		$stmt = BasePeer::doSelect($criteria, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = GradeunitPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = GradeunitPeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {

				$cls = GradeunitPeer::getOMClass(false);

				$obj1 = new $cls();
				$obj1->hydrate($row);
				GradeunitPeer::addInstanceToPool($obj1, $key1);
			} // if $obj1 already loaded

			$key2 = ProfessorPeer::getPrimaryKeyHashFromRow($row, $startcol);
			if ($key2 !== null) {
				$obj2 = ProfessorPeer::getInstanceFromPool($key2);
				if (!$obj2) {

					$cls = ProfessorPeer::getOMClass(false);

					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol);
					ProfessorPeer::addInstanceToPool($obj2, $key2);
				} // if obj2 already loaded
				
				// Add the $obj1 (Gradeunit) to $obj2 (Professor)
				$obj2->addGradeunit($obj1);

			} // if joined row was not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of Gradeunit objects pre-filled with their Periodo objects.
	 * @param      Criteria  $criteria
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of Gradeunit objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinPeriodo(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$criteria = clone $criteria;

		// Set the correct dbName if it has not been overridden
		if ($criteria->getDbName() == Propel::getDefaultDB()) {
			$criteria->setDbName(self::DATABASE_NAME);
		}

		GradeunitPeer::addSelectColumns($criteria);
		$startcol = (GradeunitPeer::NUM_COLUMNS - GradeunitPeer::NUM_LAZY_LOAD_COLUMNS);
		PeriodoPeer::addSelectColumns($criteria);

		$criteria->addJoin(GradeunitPeer::PERIODO_ID, PeriodoPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}

		$stmt = BasePeer::doSelect($criteria, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = GradeunitPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = GradeunitPeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {

				$cls = GradeunitPeer::getOMClass(false);

				$obj1 = new $cls();
				$obj1->hydrate($row);
				GradeunitPeer::addInstanceToPool($obj1, $key1);
			} // if $obj1 already loaded

			$key2 = PeriodoPeer::getPrimaryKeyHashFromRow($row, $startcol);
			if ($key2 !== null) {
				$obj2 = PeriodoPeer::getInstanceFromPool($key2);
				if (!$obj2) {

					$cls = PeriodoPeer::getOMClass(false);

					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol);
					PeriodoPeer::addInstanceToPool($obj2, $key2);
				} // if obj2 already loaded
				
				// Add the $obj1 (Gradeunit) to $obj2 (Periodo)
				$obj2->addGradeunit($obj1);

			} // if joined row was not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of Gradeunit objects pre-filled with their Local objects.
	 * @param      Criteria  $criteria
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of Gradeunit objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinLocal(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$criteria = clone $criteria;

		// Set the correct dbName if it has not been overridden
		if ($criteria->getDbName() == Propel::getDefaultDB()) {
			$criteria->setDbName(self::DATABASE_NAME);
		}

		GradeunitPeer::addSelectColumns($criteria);
		$startcol = (GradeunitPeer::NUM_COLUMNS - GradeunitPeer::NUM_LAZY_LOAD_COLUMNS);
		LocalPeer::addSelectColumns($criteria);

		$criteria->addJoin(GradeunitPeer::LOCAL_ID, LocalPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}

		$stmt = BasePeer::doSelect($criteria, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = GradeunitPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = GradeunitPeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {

				$cls = GradeunitPeer::getOMClass(false);

				$obj1 = new $cls();
				$obj1->hydrate($row);
				GradeunitPeer::addInstanceToPool($obj1, $key1);
			} // if $obj1 already loaded

			$key2 = LocalPeer::getPrimaryKeyHashFromRow($row, $startcol);
			if ($key2 !== null) {
				$obj2 = LocalPeer::getInstanceFromPool($key2);
				if (!$obj2) {

					$cls = LocalPeer::getOMClass(false);

					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol);
					LocalPeer::addInstanceToPool($obj2, $key2);
				} // if obj2 already loaded
				
				// Add the $obj1 (Gradeunit) to $obj2 (Local)
				$obj2->addGradeunit($obj1);

			} // if joined row was not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of Gradeunit objects pre-filled with their Week objects.
	 * @param      Criteria  $criteria
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of Gradeunit objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinWeek(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$criteria = clone $criteria;

		// Set the correct dbName if it has not been overridden
		if ($criteria->getDbName() == Propel::getDefaultDB()) {
			$criteria->setDbName(self::DATABASE_NAME);
		}

		GradeunitPeer::addSelectColumns($criteria);
		$startcol = (GradeunitPeer::NUM_COLUMNS - GradeunitPeer::NUM_LAZY_LOAD_COLUMNS);
		WeekPeer::addSelectColumns($criteria);

		$criteria->addJoin(GradeunitPeer::WEEKDAY_ID, WeekPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}

		$stmt = BasePeer::doSelect($criteria, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = GradeunitPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = GradeunitPeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {

				$cls = GradeunitPeer::getOMClass(false);

				$obj1 = new $cls();
				$obj1->hydrate($row);
				GradeunitPeer::addInstanceToPool($obj1, $key1);
			} // if $obj1 already loaded

			$key2 = WeekPeer::getPrimaryKeyHashFromRow($row, $startcol);
			if ($key2 !== null) {
				$obj2 = WeekPeer::getInstanceFromPool($key2);
				if (!$obj2) {

					$cls = WeekPeer::getOMClass(false);

					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol);
					WeekPeer::addInstanceToPool($obj2, $key2);
				} // if obj2 already loaded
				
				// Add the $obj1 (Gradeunit) to $obj2 (Week)
				$obj2->addGradeunit($obj1);

			} // if joined row was not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Returns the number of rows matching criteria, joining all related tables
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinAll(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(GradeunitPeer::TABLE_NAME);

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			GradeunitPeer::addSelectColumns($criteria);
		}
		
		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count
		
		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(GradeunitPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$criteria->addJoin(GradeunitPeer::HORARIO_ID, HorarioPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::DISCIPLINA_ID, DisciplinaPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PROFESSOR_ID, ProfessorPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PERIODO_ID, PeriodoPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::LOCAL_ID, LocalPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::WEEKDAY_ID, WeekPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}

		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}

	/**
	 * Selects a collection of Gradeunit objects pre-filled with all related objects.
	 *
	 * @param      Criteria  $criteria
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of Gradeunit objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinAll(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$criteria = clone $criteria;

		// Set the correct dbName if it has not been overridden
		if ($criteria->getDbName() == Propel::getDefaultDB()) {
			$criteria->setDbName(self::DATABASE_NAME);
		}

		GradeunitPeer::addSelectColumns($criteria);
		$startcol2 = (GradeunitPeer::NUM_COLUMNS - GradeunitPeer::NUM_LAZY_LOAD_COLUMNS);

		HorarioPeer::addSelectColumns($criteria);
		$startcol3 = $startcol2 + (HorarioPeer::NUM_COLUMNS - HorarioPeer::NUM_LAZY_LOAD_COLUMNS);

		DisciplinaPeer::addSelectColumns($criteria);
		$startcol4 = $startcol3 + (DisciplinaPeer::NUM_COLUMNS - DisciplinaPeer::NUM_LAZY_LOAD_COLUMNS);

		ProfessorPeer::addSelectColumns($criteria);
		$startcol5 = $startcol4 + (ProfessorPeer::NUM_COLUMNS - ProfessorPeer::NUM_LAZY_LOAD_COLUMNS);

		PeriodoPeer::addSelectColumns($criteria);
		$startcol6 = $startcol5 + (PeriodoPeer::NUM_COLUMNS - PeriodoPeer::NUM_LAZY_LOAD_COLUMNS);

		LocalPeer::addSelectColumns($criteria);
		$startcol7 = $startcol6 + (LocalPeer::NUM_COLUMNS - LocalPeer::NUM_LAZY_LOAD_COLUMNS);

		WeekPeer::addSelectColumns($criteria);
		$startcol8 = $startcol7 + (WeekPeer::NUM_COLUMNS - WeekPeer::NUM_LAZY_LOAD_COLUMNS);

		$criteria->addJoin(GradeunitPeer::HORARIO_ID, HorarioPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::DISCIPLINA_ID, DisciplinaPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PROFESSOR_ID, ProfessorPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PERIODO_ID, PeriodoPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::LOCAL_ID, LocalPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::WEEKDAY_ID, WeekPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}

		$stmt = BasePeer::doSelect($criteria, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = GradeunitPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = GradeunitPeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {
				$cls = GradeunitPeer::getOMClass(false);

				$obj1 = new $cls();
				$obj1->hydrate($row);
				GradeunitPeer::addInstanceToPool($obj1, $key1);
			} // if obj1 already loaded

			// Add objects for joined Horario rows

			$key2 = HorarioPeer::getPrimaryKeyHashFromRow($row, $startcol2);
			if ($key2 !== null) {
				$obj2 = HorarioPeer::getInstanceFromPool($key2);
				if (!$obj2) {

					$cls = HorarioPeer::getOMClass(false);

					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol2);
					HorarioPeer::addInstanceToPool($obj2, $key2);
				} // if obj2 loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj2 (Horario)
				$obj2->addGradeunit($obj1);
			} // if joined row not null

			// Add objects for joined Disciplina rows

			$key3 = DisciplinaPeer::getPrimaryKeyHashFromRow($row, $startcol3);
			if ($key3 !== null) {
				$obj3 = DisciplinaPeer::getInstanceFromPool($key3);
				if (!$obj3) {

					$cls = DisciplinaPeer::getOMClass(false);

					$obj3 = new $cls();
					$obj3->hydrate($row, $startcol3);
					DisciplinaPeer::addInstanceToPool($obj3, $key3);
				} // if obj3 loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj3 (Disciplina)
				$obj3->addGradeunit($obj1);
			} // if joined row not null

			// Add objects for joined Professor rows

			$key4 = ProfessorPeer::getPrimaryKeyHashFromRow($row, $startcol4);
			if ($key4 !== null) {
				$obj4 = ProfessorPeer::getInstanceFromPool($key4);
				if (!$obj4) {

					$cls = ProfessorPeer::getOMClass(false);

					$obj4 = new $cls();
					$obj4->hydrate($row, $startcol4);
					ProfessorPeer::addInstanceToPool($obj4, $key4);
				} // if obj4 loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj4 (Professor)
				$obj4->addGradeunit($obj1);
			} // if joined row not null

			// Add objects for joined Periodo rows

			$key5 = PeriodoPeer::getPrimaryKeyHashFromRow($row, $startcol5);
			if ($key5 !== null) {
				$obj5 = PeriodoPeer::getInstanceFromPool($key5);
				if (!$obj5) {

					$cls = PeriodoPeer::getOMClass(false);

					$obj5 = new $cls();
					$obj5->hydrate($row, $startcol5);
					PeriodoPeer::addInstanceToPool($obj5, $key5);
				} // if obj5 loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj5 (Periodo)
				$obj5->addGradeunit($obj1);
			} // if joined row not null

			// Add objects for joined Local rows

			$key6 = LocalPeer::getPrimaryKeyHashFromRow($row, $startcol6);
			if ($key6 !== null) {
				$obj6 = LocalPeer::getInstanceFromPool($key6);
				if (!$obj6) {

					$cls = LocalPeer::getOMClass(false);

					$obj6 = new $cls();
					$obj6->hydrate($row, $startcol6);
					LocalPeer::addInstanceToPool($obj6, $key6);
				} // if obj6 loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj6 (Local)
				$obj6->addGradeunit($obj1);
			} // if joined row not null

			// Add objects for joined Week rows

			$key7 = WeekPeer::getPrimaryKeyHashFromRow($row, $startcol7);
			if ($key7 !== null) {
				$obj7 = WeekPeer::getInstanceFromPool($key7);
				if (!$obj7) {

					$cls = WeekPeer::getOMClass(false);

					$obj7 = new $cls();
					$obj7->hydrate($row, $startcol7);
					WeekPeer::addInstanceToPool($obj7, $key7);
				} // if obj7 loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj7 (Week)
				$obj7->addGradeunit($obj1);
			} // if joined row not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related Horario table
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinAllExceptHorario(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(GradeunitPeer::TABLE_NAME);
		
		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			GradeunitPeer::addSelectColumns($criteria);
		}
		
		$criteria->clearOrderByColumns(); // ORDER BY should not affect count
		
		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(GradeunitPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}
	
		$criteria->addJoin(GradeunitPeer::DISCIPLINA_ID, DisciplinaPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PROFESSOR_ID, ProfessorPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PERIODO_ID, PeriodoPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::LOCAL_ID, LocalPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::WEEKDAY_ID, WeekPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}

		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related Disciplina table
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinAllExceptDisciplina(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(GradeunitPeer::TABLE_NAME);
		
		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			GradeunitPeer::addSelectColumns($criteria);
		}
		
		$criteria->clearOrderByColumns(); // ORDER BY should not affect count
		
		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(GradeunitPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}
	
		$criteria->addJoin(GradeunitPeer::HORARIO_ID, HorarioPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PROFESSOR_ID, ProfessorPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PERIODO_ID, PeriodoPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::LOCAL_ID, LocalPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::WEEKDAY_ID, WeekPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}

		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related Professor table
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinAllExceptProfessor(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(GradeunitPeer::TABLE_NAME);
		
		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			GradeunitPeer::addSelectColumns($criteria);
		}
		
		$criteria->clearOrderByColumns(); // ORDER BY should not affect count
		
		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(GradeunitPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}
	
		$criteria->addJoin(GradeunitPeer::HORARIO_ID, HorarioPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::DISCIPLINA_ID, DisciplinaPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PERIODO_ID, PeriodoPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::LOCAL_ID, LocalPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::WEEKDAY_ID, WeekPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}

		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related Periodo table
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinAllExceptPeriodo(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(GradeunitPeer::TABLE_NAME);
		
		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			GradeunitPeer::addSelectColumns($criteria);
		}
		
		$criteria->clearOrderByColumns(); // ORDER BY should not affect count
		
		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(GradeunitPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}
	
		$criteria->addJoin(GradeunitPeer::HORARIO_ID, HorarioPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::DISCIPLINA_ID, DisciplinaPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PROFESSOR_ID, ProfessorPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::LOCAL_ID, LocalPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::WEEKDAY_ID, WeekPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}

		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related Local table
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinAllExceptLocal(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(GradeunitPeer::TABLE_NAME);
		
		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			GradeunitPeer::addSelectColumns($criteria);
		}
		
		$criteria->clearOrderByColumns(); // ORDER BY should not affect count
		
		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(GradeunitPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}
	
		$criteria->addJoin(GradeunitPeer::HORARIO_ID, HorarioPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::DISCIPLINA_ID, DisciplinaPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PROFESSOR_ID, ProfessorPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PERIODO_ID, PeriodoPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::WEEKDAY_ID, WeekPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}

		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related Week table
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinAllExceptWeek(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(GradeunitPeer::TABLE_NAME);
		
		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			GradeunitPeer::addSelectColumns($criteria);
		}
		
		$criteria->clearOrderByColumns(); // ORDER BY should not affect count
		
		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(GradeunitPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}
	
		$criteria->addJoin(GradeunitPeer::HORARIO_ID, HorarioPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::DISCIPLINA_ID, DisciplinaPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PROFESSOR_ID, ProfessorPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PERIODO_ID, PeriodoPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::LOCAL_ID, LocalPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}

		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Selects a collection of Gradeunit objects pre-filled with all related objects except Horario.
	 *
	 * @param      Criteria  $criteria
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of Gradeunit objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinAllExceptHorario(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$criteria = clone $criteria;

		// Set the correct dbName if it has not been overridden
		// $criteria->getDbName() will return the same object if not set to another value
		// so == check is okay and faster
		if ($criteria->getDbName() == Propel::getDefaultDB()) {
			$criteria->setDbName(self::DATABASE_NAME);
		}

		GradeunitPeer::addSelectColumns($criteria);
		$startcol2 = (GradeunitPeer::NUM_COLUMNS - GradeunitPeer::NUM_LAZY_LOAD_COLUMNS);

		DisciplinaPeer::addSelectColumns($criteria);
		$startcol3 = $startcol2 + (DisciplinaPeer::NUM_COLUMNS - DisciplinaPeer::NUM_LAZY_LOAD_COLUMNS);

		ProfessorPeer::addSelectColumns($criteria);
		$startcol4 = $startcol3 + (ProfessorPeer::NUM_COLUMNS - ProfessorPeer::NUM_LAZY_LOAD_COLUMNS);

		PeriodoPeer::addSelectColumns($criteria);
		$startcol5 = $startcol4 + (PeriodoPeer::NUM_COLUMNS - PeriodoPeer::NUM_LAZY_LOAD_COLUMNS);

		LocalPeer::addSelectColumns($criteria);
		$startcol6 = $startcol5 + (LocalPeer::NUM_COLUMNS - LocalPeer::NUM_LAZY_LOAD_COLUMNS);

		WeekPeer::addSelectColumns($criteria);
		$startcol7 = $startcol6 + (WeekPeer::NUM_COLUMNS - WeekPeer::NUM_LAZY_LOAD_COLUMNS);

		$criteria->addJoin(GradeunitPeer::DISCIPLINA_ID, DisciplinaPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PROFESSOR_ID, ProfessorPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PERIODO_ID, PeriodoPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::LOCAL_ID, LocalPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::WEEKDAY_ID, WeekPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}


		$stmt = BasePeer::doSelect($criteria, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = GradeunitPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = GradeunitPeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {
				$cls = GradeunitPeer::getOMClass(false);

				$obj1 = new $cls();
				$obj1->hydrate($row);
				GradeunitPeer::addInstanceToPool($obj1, $key1);
			} // if obj1 already loaded

				// Add objects for joined Disciplina rows

				$key2 = DisciplinaPeer::getPrimaryKeyHashFromRow($row, $startcol2);
				if ($key2 !== null) {
					$obj2 = DisciplinaPeer::getInstanceFromPool($key2);
					if (!$obj2) {
	
						$cls = DisciplinaPeer::getOMClass(false);

					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol2);
					DisciplinaPeer::addInstanceToPool($obj2, $key2);
				} // if $obj2 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj2 (Disciplina)
				$obj2->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Professor rows

				$key3 = ProfessorPeer::getPrimaryKeyHashFromRow($row, $startcol3);
				if ($key3 !== null) {
					$obj3 = ProfessorPeer::getInstanceFromPool($key3);
					if (!$obj3) {
	
						$cls = ProfessorPeer::getOMClass(false);

					$obj3 = new $cls();
					$obj3->hydrate($row, $startcol3);
					ProfessorPeer::addInstanceToPool($obj3, $key3);
				} // if $obj3 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj3 (Professor)
				$obj3->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Periodo rows

				$key4 = PeriodoPeer::getPrimaryKeyHashFromRow($row, $startcol4);
				if ($key4 !== null) {
					$obj4 = PeriodoPeer::getInstanceFromPool($key4);
					if (!$obj4) {
	
						$cls = PeriodoPeer::getOMClass(false);

					$obj4 = new $cls();
					$obj4->hydrate($row, $startcol4);
					PeriodoPeer::addInstanceToPool($obj4, $key4);
				} // if $obj4 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj4 (Periodo)
				$obj4->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Local rows

				$key5 = LocalPeer::getPrimaryKeyHashFromRow($row, $startcol5);
				if ($key5 !== null) {
					$obj5 = LocalPeer::getInstanceFromPool($key5);
					if (!$obj5) {
	
						$cls = LocalPeer::getOMClass(false);

					$obj5 = new $cls();
					$obj5->hydrate($row, $startcol5);
					LocalPeer::addInstanceToPool($obj5, $key5);
				} // if $obj5 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj5 (Local)
				$obj5->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Week rows

				$key6 = WeekPeer::getPrimaryKeyHashFromRow($row, $startcol6);
				if ($key6 !== null) {
					$obj6 = WeekPeer::getInstanceFromPool($key6);
					if (!$obj6) {
	
						$cls = WeekPeer::getOMClass(false);

					$obj6 = new $cls();
					$obj6->hydrate($row, $startcol6);
					WeekPeer::addInstanceToPool($obj6, $key6);
				} // if $obj6 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj6 (Week)
				$obj6->addGradeunit($obj1);

			} // if joined row is not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of Gradeunit objects pre-filled with all related objects except Disciplina.
	 *
	 * @param      Criteria  $criteria
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of Gradeunit objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinAllExceptDisciplina(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$criteria = clone $criteria;

		// Set the correct dbName if it has not been overridden
		// $criteria->getDbName() will return the same object if not set to another value
		// so == check is okay and faster
		if ($criteria->getDbName() == Propel::getDefaultDB()) {
			$criteria->setDbName(self::DATABASE_NAME);
		}

		GradeunitPeer::addSelectColumns($criteria);
		$startcol2 = (GradeunitPeer::NUM_COLUMNS - GradeunitPeer::NUM_LAZY_LOAD_COLUMNS);

		HorarioPeer::addSelectColumns($criteria);
		$startcol3 = $startcol2 + (HorarioPeer::NUM_COLUMNS - HorarioPeer::NUM_LAZY_LOAD_COLUMNS);

		ProfessorPeer::addSelectColumns($criteria);
		$startcol4 = $startcol3 + (ProfessorPeer::NUM_COLUMNS - ProfessorPeer::NUM_LAZY_LOAD_COLUMNS);

		PeriodoPeer::addSelectColumns($criteria);
		$startcol5 = $startcol4 + (PeriodoPeer::NUM_COLUMNS - PeriodoPeer::NUM_LAZY_LOAD_COLUMNS);

		LocalPeer::addSelectColumns($criteria);
		$startcol6 = $startcol5 + (LocalPeer::NUM_COLUMNS - LocalPeer::NUM_LAZY_LOAD_COLUMNS);

		WeekPeer::addSelectColumns($criteria);
		$startcol7 = $startcol6 + (WeekPeer::NUM_COLUMNS - WeekPeer::NUM_LAZY_LOAD_COLUMNS);

		$criteria->addJoin(GradeunitPeer::HORARIO_ID, HorarioPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PROFESSOR_ID, ProfessorPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PERIODO_ID, PeriodoPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::LOCAL_ID, LocalPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::WEEKDAY_ID, WeekPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}


		$stmt = BasePeer::doSelect($criteria, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = GradeunitPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = GradeunitPeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {
				$cls = GradeunitPeer::getOMClass(false);

				$obj1 = new $cls();
				$obj1->hydrate($row);
				GradeunitPeer::addInstanceToPool($obj1, $key1);
			} // if obj1 already loaded

				// Add objects for joined Horario rows

				$key2 = HorarioPeer::getPrimaryKeyHashFromRow($row, $startcol2);
				if ($key2 !== null) {
					$obj2 = HorarioPeer::getInstanceFromPool($key2);
					if (!$obj2) {
	
						$cls = HorarioPeer::getOMClass(false);

					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol2);
					HorarioPeer::addInstanceToPool($obj2, $key2);
				} // if $obj2 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj2 (Horario)
				$obj2->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Professor rows

				$key3 = ProfessorPeer::getPrimaryKeyHashFromRow($row, $startcol3);
				if ($key3 !== null) {
					$obj3 = ProfessorPeer::getInstanceFromPool($key3);
					if (!$obj3) {
	
						$cls = ProfessorPeer::getOMClass(false);

					$obj3 = new $cls();
					$obj3->hydrate($row, $startcol3);
					ProfessorPeer::addInstanceToPool($obj3, $key3);
				} // if $obj3 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj3 (Professor)
				$obj3->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Periodo rows

				$key4 = PeriodoPeer::getPrimaryKeyHashFromRow($row, $startcol4);
				if ($key4 !== null) {
					$obj4 = PeriodoPeer::getInstanceFromPool($key4);
					if (!$obj4) {
	
						$cls = PeriodoPeer::getOMClass(false);

					$obj4 = new $cls();
					$obj4->hydrate($row, $startcol4);
					PeriodoPeer::addInstanceToPool($obj4, $key4);
				} // if $obj4 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj4 (Periodo)
				$obj4->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Local rows

				$key5 = LocalPeer::getPrimaryKeyHashFromRow($row, $startcol5);
				if ($key5 !== null) {
					$obj5 = LocalPeer::getInstanceFromPool($key5);
					if (!$obj5) {
	
						$cls = LocalPeer::getOMClass(false);

					$obj5 = new $cls();
					$obj5->hydrate($row, $startcol5);
					LocalPeer::addInstanceToPool($obj5, $key5);
				} // if $obj5 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj5 (Local)
				$obj5->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Week rows

				$key6 = WeekPeer::getPrimaryKeyHashFromRow($row, $startcol6);
				if ($key6 !== null) {
					$obj6 = WeekPeer::getInstanceFromPool($key6);
					if (!$obj6) {
	
						$cls = WeekPeer::getOMClass(false);

					$obj6 = new $cls();
					$obj6->hydrate($row, $startcol6);
					WeekPeer::addInstanceToPool($obj6, $key6);
				} // if $obj6 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj6 (Week)
				$obj6->addGradeunit($obj1);

			} // if joined row is not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of Gradeunit objects pre-filled with all related objects except Professor.
	 *
	 * @param      Criteria  $criteria
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of Gradeunit objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinAllExceptProfessor(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$criteria = clone $criteria;

		// Set the correct dbName if it has not been overridden
		// $criteria->getDbName() will return the same object if not set to another value
		// so == check is okay and faster
		if ($criteria->getDbName() == Propel::getDefaultDB()) {
			$criteria->setDbName(self::DATABASE_NAME);
		}

		GradeunitPeer::addSelectColumns($criteria);
		$startcol2 = (GradeunitPeer::NUM_COLUMNS - GradeunitPeer::NUM_LAZY_LOAD_COLUMNS);

		HorarioPeer::addSelectColumns($criteria);
		$startcol3 = $startcol2 + (HorarioPeer::NUM_COLUMNS - HorarioPeer::NUM_LAZY_LOAD_COLUMNS);

		DisciplinaPeer::addSelectColumns($criteria);
		$startcol4 = $startcol3 + (DisciplinaPeer::NUM_COLUMNS - DisciplinaPeer::NUM_LAZY_LOAD_COLUMNS);

		PeriodoPeer::addSelectColumns($criteria);
		$startcol5 = $startcol4 + (PeriodoPeer::NUM_COLUMNS - PeriodoPeer::NUM_LAZY_LOAD_COLUMNS);

		LocalPeer::addSelectColumns($criteria);
		$startcol6 = $startcol5 + (LocalPeer::NUM_COLUMNS - LocalPeer::NUM_LAZY_LOAD_COLUMNS);

		WeekPeer::addSelectColumns($criteria);
		$startcol7 = $startcol6 + (WeekPeer::NUM_COLUMNS - WeekPeer::NUM_LAZY_LOAD_COLUMNS);

		$criteria->addJoin(GradeunitPeer::HORARIO_ID, HorarioPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::DISCIPLINA_ID, DisciplinaPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PERIODO_ID, PeriodoPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::LOCAL_ID, LocalPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::WEEKDAY_ID, WeekPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}


		$stmt = BasePeer::doSelect($criteria, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = GradeunitPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = GradeunitPeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {
				$cls = GradeunitPeer::getOMClass(false);

				$obj1 = new $cls();
				$obj1->hydrate($row);
				GradeunitPeer::addInstanceToPool($obj1, $key1);
			} // if obj1 already loaded

				// Add objects for joined Horario rows

				$key2 = HorarioPeer::getPrimaryKeyHashFromRow($row, $startcol2);
				if ($key2 !== null) {
					$obj2 = HorarioPeer::getInstanceFromPool($key2);
					if (!$obj2) {
	
						$cls = HorarioPeer::getOMClass(false);

					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol2);
					HorarioPeer::addInstanceToPool($obj2, $key2);
				} // if $obj2 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj2 (Horario)
				$obj2->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Disciplina rows

				$key3 = DisciplinaPeer::getPrimaryKeyHashFromRow($row, $startcol3);
				if ($key3 !== null) {
					$obj3 = DisciplinaPeer::getInstanceFromPool($key3);
					if (!$obj3) {
	
						$cls = DisciplinaPeer::getOMClass(false);

					$obj3 = new $cls();
					$obj3->hydrate($row, $startcol3);
					DisciplinaPeer::addInstanceToPool($obj3, $key3);
				} // if $obj3 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj3 (Disciplina)
				$obj3->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Periodo rows

				$key4 = PeriodoPeer::getPrimaryKeyHashFromRow($row, $startcol4);
				if ($key4 !== null) {
					$obj4 = PeriodoPeer::getInstanceFromPool($key4);
					if (!$obj4) {
	
						$cls = PeriodoPeer::getOMClass(false);

					$obj4 = new $cls();
					$obj4->hydrate($row, $startcol4);
					PeriodoPeer::addInstanceToPool($obj4, $key4);
				} // if $obj4 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj4 (Periodo)
				$obj4->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Local rows

				$key5 = LocalPeer::getPrimaryKeyHashFromRow($row, $startcol5);
				if ($key5 !== null) {
					$obj5 = LocalPeer::getInstanceFromPool($key5);
					if (!$obj5) {
	
						$cls = LocalPeer::getOMClass(false);

					$obj5 = new $cls();
					$obj5->hydrate($row, $startcol5);
					LocalPeer::addInstanceToPool($obj5, $key5);
				} // if $obj5 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj5 (Local)
				$obj5->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Week rows

				$key6 = WeekPeer::getPrimaryKeyHashFromRow($row, $startcol6);
				if ($key6 !== null) {
					$obj6 = WeekPeer::getInstanceFromPool($key6);
					if (!$obj6) {
	
						$cls = WeekPeer::getOMClass(false);

					$obj6 = new $cls();
					$obj6->hydrate($row, $startcol6);
					WeekPeer::addInstanceToPool($obj6, $key6);
				} // if $obj6 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj6 (Week)
				$obj6->addGradeunit($obj1);

			} // if joined row is not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of Gradeunit objects pre-filled with all related objects except Periodo.
	 *
	 * @param      Criteria  $criteria
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of Gradeunit objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinAllExceptPeriodo(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$criteria = clone $criteria;

		// Set the correct dbName if it has not been overridden
		// $criteria->getDbName() will return the same object if not set to another value
		// so == check is okay and faster
		if ($criteria->getDbName() == Propel::getDefaultDB()) {
			$criteria->setDbName(self::DATABASE_NAME);
		}

		GradeunitPeer::addSelectColumns($criteria);
		$startcol2 = (GradeunitPeer::NUM_COLUMNS - GradeunitPeer::NUM_LAZY_LOAD_COLUMNS);

		HorarioPeer::addSelectColumns($criteria);
		$startcol3 = $startcol2 + (HorarioPeer::NUM_COLUMNS - HorarioPeer::NUM_LAZY_LOAD_COLUMNS);

		DisciplinaPeer::addSelectColumns($criteria);
		$startcol4 = $startcol3 + (DisciplinaPeer::NUM_COLUMNS - DisciplinaPeer::NUM_LAZY_LOAD_COLUMNS);

		ProfessorPeer::addSelectColumns($criteria);
		$startcol5 = $startcol4 + (ProfessorPeer::NUM_COLUMNS - ProfessorPeer::NUM_LAZY_LOAD_COLUMNS);

		LocalPeer::addSelectColumns($criteria);
		$startcol6 = $startcol5 + (LocalPeer::NUM_COLUMNS - LocalPeer::NUM_LAZY_LOAD_COLUMNS);

		WeekPeer::addSelectColumns($criteria);
		$startcol7 = $startcol6 + (WeekPeer::NUM_COLUMNS - WeekPeer::NUM_LAZY_LOAD_COLUMNS);

		$criteria->addJoin(GradeunitPeer::HORARIO_ID, HorarioPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::DISCIPLINA_ID, DisciplinaPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PROFESSOR_ID, ProfessorPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::LOCAL_ID, LocalPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::WEEKDAY_ID, WeekPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}


		$stmt = BasePeer::doSelect($criteria, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = GradeunitPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = GradeunitPeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {
				$cls = GradeunitPeer::getOMClass(false);

				$obj1 = new $cls();
				$obj1->hydrate($row);
				GradeunitPeer::addInstanceToPool($obj1, $key1);
			} // if obj1 already loaded

				// Add objects for joined Horario rows

				$key2 = HorarioPeer::getPrimaryKeyHashFromRow($row, $startcol2);
				if ($key2 !== null) {
					$obj2 = HorarioPeer::getInstanceFromPool($key2);
					if (!$obj2) {
	
						$cls = HorarioPeer::getOMClass(false);

					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol2);
					HorarioPeer::addInstanceToPool($obj2, $key2);
				} // if $obj2 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj2 (Horario)
				$obj2->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Disciplina rows

				$key3 = DisciplinaPeer::getPrimaryKeyHashFromRow($row, $startcol3);
				if ($key3 !== null) {
					$obj3 = DisciplinaPeer::getInstanceFromPool($key3);
					if (!$obj3) {
	
						$cls = DisciplinaPeer::getOMClass(false);

					$obj3 = new $cls();
					$obj3->hydrate($row, $startcol3);
					DisciplinaPeer::addInstanceToPool($obj3, $key3);
				} // if $obj3 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj3 (Disciplina)
				$obj3->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Professor rows

				$key4 = ProfessorPeer::getPrimaryKeyHashFromRow($row, $startcol4);
				if ($key4 !== null) {
					$obj4 = ProfessorPeer::getInstanceFromPool($key4);
					if (!$obj4) {
	
						$cls = ProfessorPeer::getOMClass(false);

					$obj4 = new $cls();
					$obj4->hydrate($row, $startcol4);
					ProfessorPeer::addInstanceToPool($obj4, $key4);
				} // if $obj4 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj4 (Professor)
				$obj4->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Local rows

				$key5 = LocalPeer::getPrimaryKeyHashFromRow($row, $startcol5);
				if ($key5 !== null) {
					$obj5 = LocalPeer::getInstanceFromPool($key5);
					if (!$obj5) {
	
						$cls = LocalPeer::getOMClass(false);

					$obj5 = new $cls();
					$obj5->hydrate($row, $startcol5);
					LocalPeer::addInstanceToPool($obj5, $key5);
				} // if $obj5 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj5 (Local)
				$obj5->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Week rows

				$key6 = WeekPeer::getPrimaryKeyHashFromRow($row, $startcol6);
				if ($key6 !== null) {
					$obj6 = WeekPeer::getInstanceFromPool($key6);
					if (!$obj6) {
	
						$cls = WeekPeer::getOMClass(false);

					$obj6 = new $cls();
					$obj6->hydrate($row, $startcol6);
					WeekPeer::addInstanceToPool($obj6, $key6);
				} // if $obj6 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj6 (Week)
				$obj6->addGradeunit($obj1);

			} // if joined row is not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of Gradeunit objects pre-filled with all related objects except Local.
	 *
	 * @param      Criteria  $criteria
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of Gradeunit objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinAllExceptLocal(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$criteria = clone $criteria;

		// Set the correct dbName if it has not been overridden
		// $criteria->getDbName() will return the same object if not set to another value
		// so == check is okay and faster
		if ($criteria->getDbName() == Propel::getDefaultDB()) {
			$criteria->setDbName(self::DATABASE_NAME);
		}

		GradeunitPeer::addSelectColumns($criteria);
		$startcol2 = (GradeunitPeer::NUM_COLUMNS - GradeunitPeer::NUM_LAZY_LOAD_COLUMNS);

		HorarioPeer::addSelectColumns($criteria);
		$startcol3 = $startcol2 + (HorarioPeer::NUM_COLUMNS - HorarioPeer::NUM_LAZY_LOAD_COLUMNS);

		DisciplinaPeer::addSelectColumns($criteria);
		$startcol4 = $startcol3 + (DisciplinaPeer::NUM_COLUMNS - DisciplinaPeer::NUM_LAZY_LOAD_COLUMNS);

		ProfessorPeer::addSelectColumns($criteria);
		$startcol5 = $startcol4 + (ProfessorPeer::NUM_COLUMNS - ProfessorPeer::NUM_LAZY_LOAD_COLUMNS);

		PeriodoPeer::addSelectColumns($criteria);
		$startcol6 = $startcol5 + (PeriodoPeer::NUM_COLUMNS - PeriodoPeer::NUM_LAZY_LOAD_COLUMNS);

		WeekPeer::addSelectColumns($criteria);
		$startcol7 = $startcol6 + (WeekPeer::NUM_COLUMNS - WeekPeer::NUM_LAZY_LOAD_COLUMNS);

		$criteria->addJoin(GradeunitPeer::HORARIO_ID, HorarioPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::DISCIPLINA_ID, DisciplinaPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PROFESSOR_ID, ProfessorPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PERIODO_ID, PeriodoPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::WEEKDAY_ID, WeekPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}


		$stmt = BasePeer::doSelect($criteria, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = GradeunitPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = GradeunitPeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {
				$cls = GradeunitPeer::getOMClass(false);

				$obj1 = new $cls();
				$obj1->hydrate($row);
				GradeunitPeer::addInstanceToPool($obj1, $key1);
			} // if obj1 already loaded

				// Add objects for joined Horario rows

				$key2 = HorarioPeer::getPrimaryKeyHashFromRow($row, $startcol2);
				if ($key2 !== null) {
					$obj2 = HorarioPeer::getInstanceFromPool($key2);
					if (!$obj2) {
	
						$cls = HorarioPeer::getOMClass(false);

					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol2);
					HorarioPeer::addInstanceToPool($obj2, $key2);
				} // if $obj2 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj2 (Horario)
				$obj2->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Disciplina rows

				$key3 = DisciplinaPeer::getPrimaryKeyHashFromRow($row, $startcol3);
				if ($key3 !== null) {
					$obj3 = DisciplinaPeer::getInstanceFromPool($key3);
					if (!$obj3) {
	
						$cls = DisciplinaPeer::getOMClass(false);

					$obj3 = new $cls();
					$obj3->hydrate($row, $startcol3);
					DisciplinaPeer::addInstanceToPool($obj3, $key3);
				} // if $obj3 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj3 (Disciplina)
				$obj3->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Professor rows

				$key4 = ProfessorPeer::getPrimaryKeyHashFromRow($row, $startcol4);
				if ($key4 !== null) {
					$obj4 = ProfessorPeer::getInstanceFromPool($key4);
					if (!$obj4) {
	
						$cls = ProfessorPeer::getOMClass(false);

					$obj4 = new $cls();
					$obj4->hydrate($row, $startcol4);
					ProfessorPeer::addInstanceToPool($obj4, $key4);
				} // if $obj4 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj4 (Professor)
				$obj4->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Periodo rows

				$key5 = PeriodoPeer::getPrimaryKeyHashFromRow($row, $startcol5);
				if ($key5 !== null) {
					$obj5 = PeriodoPeer::getInstanceFromPool($key5);
					if (!$obj5) {
	
						$cls = PeriodoPeer::getOMClass(false);

					$obj5 = new $cls();
					$obj5->hydrate($row, $startcol5);
					PeriodoPeer::addInstanceToPool($obj5, $key5);
				} // if $obj5 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj5 (Periodo)
				$obj5->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Week rows

				$key6 = WeekPeer::getPrimaryKeyHashFromRow($row, $startcol6);
				if ($key6 !== null) {
					$obj6 = WeekPeer::getInstanceFromPool($key6);
					if (!$obj6) {
	
						$cls = WeekPeer::getOMClass(false);

					$obj6 = new $cls();
					$obj6->hydrate($row, $startcol6);
					WeekPeer::addInstanceToPool($obj6, $key6);
				} // if $obj6 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj6 (Week)
				$obj6->addGradeunit($obj1);

			} // if joined row is not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of Gradeunit objects pre-filled with all related objects except Week.
	 *
	 * @param      Criteria  $criteria
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of Gradeunit objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinAllExceptWeek(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$criteria = clone $criteria;

		// Set the correct dbName if it has not been overridden
		// $criteria->getDbName() will return the same object if not set to another value
		// so == check is okay and faster
		if ($criteria->getDbName() == Propel::getDefaultDB()) {
			$criteria->setDbName(self::DATABASE_NAME);
		}

		GradeunitPeer::addSelectColumns($criteria);
		$startcol2 = (GradeunitPeer::NUM_COLUMNS - GradeunitPeer::NUM_LAZY_LOAD_COLUMNS);

		HorarioPeer::addSelectColumns($criteria);
		$startcol3 = $startcol2 + (HorarioPeer::NUM_COLUMNS - HorarioPeer::NUM_LAZY_LOAD_COLUMNS);

		DisciplinaPeer::addSelectColumns($criteria);
		$startcol4 = $startcol3 + (DisciplinaPeer::NUM_COLUMNS - DisciplinaPeer::NUM_LAZY_LOAD_COLUMNS);

		ProfessorPeer::addSelectColumns($criteria);
		$startcol5 = $startcol4 + (ProfessorPeer::NUM_COLUMNS - ProfessorPeer::NUM_LAZY_LOAD_COLUMNS);

		PeriodoPeer::addSelectColumns($criteria);
		$startcol6 = $startcol5 + (PeriodoPeer::NUM_COLUMNS - PeriodoPeer::NUM_LAZY_LOAD_COLUMNS);

		LocalPeer::addSelectColumns($criteria);
		$startcol7 = $startcol6 + (LocalPeer::NUM_COLUMNS - LocalPeer::NUM_LAZY_LOAD_COLUMNS);

		$criteria->addJoin(GradeunitPeer::HORARIO_ID, HorarioPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::DISCIPLINA_ID, DisciplinaPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PROFESSOR_ID, ProfessorPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::PERIODO_ID, PeriodoPeer::ID, $join_behavior);

		$criteria->addJoin(GradeunitPeer::LOCAL_ID, LocalPeer::ID, $join_behavior);

		// symfony_behaviors behavior
		foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
		{
		  call_user_func($sf_hook, 'BaseGradeunitPeer', $criteria, $con);
		}


		$stmt = BasePeer::doSelect($criteria, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = GradeunitPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = GradeunitPeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {
				$cls = GradeunitPeer::getOMClass(false);

				$obj1 = new $cls();
				$obj1->hydrate($row);
				GradeunitPeer::addInstanceToPool($obj1, $key1);
			} // if obj1 already loaded

				// Add objects for joined Horario rows

				$key2 = HorarioPeer::getPrimaryKeyHashFromRow($row, $startcol2);
				if ($key2 !== null) {
					$obj2 = HorarioPeer::getInstanceFromPool($key2);
					if (!$obj2) {
	
						$cls = HorarioPeer::getOMClass(false);

					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol2);
					HorarioPeer::addInstanceToPool($obj2, $key2);
				} // if $obj2 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj2 (Horario)
				$obj2->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Disciplina rows

				$key3 = DisciplinaPeer::getPrimaryKeyHashFromRow($row, $startcol3);
				if ($key3 !== null) {
					$obj3 = DisciplinaPeer::getInstanceFromPool($key3);
					if (!$obj3) {
	
						$cls = DisciplinaPeer::getOMClass(false);

					$obj3 = new $cls();
					$obj3->hydrate($row, $startcol3);
					DisciplinaPeer::addInstanceToPool($obj3, $key3);
				} // if $obj3 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj3 (Disciplina)
				$obj3->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Professor rows

				$key4 = ProfessorPeer::getPrimaryKeyHashFromRow($row, $startcol4);
				if ($key4 !== null) {
					$obj4 = ProfessorPeer::getInstanceFromPool($key4);
					if (!$obj4) {
	
						$cls = ProfessorPeer::getOMClass(false);

					$obj4 = new $cls();
					$obj4->hydrate($row, $startcol4);
					ProfessorPeer::addInstanceToPool($obj4, $key4);
				} // if $obj4 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj4 (Professor)
				$obj4->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Periodo rows

				$key5 = PeriodoPeer::getPrimaryKeyHashFromRow($row, $startcol5);
				if ($key5 !== null) {
					$obj5 = PeriodoPeer::getInstanceFromPool($key5);
					if (!$obj5) {
	
						$cls = PeriodoPeer::getOMClass(false);

					$obj5 = new $cls();
					$obj5->hydrate($row, $startcol5);
					PeriodoPeer::addInstanceToPool($obj5, $key5);
				} // if $obj5 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj5 (Periodo)
				$obj5->addGradeunit($obj1);

			} // if joined row is not null

				// Add objects for joined Local rows

				$key6 = LocalPeer::getPrimaryKeyHashFromRow($row, $startcol6);
				if ($key6 !== null) {
					$obj6 = LocalPeer::getInstanceFromPool($key6);
					if (!$obj6) {
	
						$cls = LocalPeer::getOMClass(false);

					$obj6 = new $cls();
					$obj6->hydrate($row, $startcol6);
					LocalPeer::addInstanceToPool($obj6, $key6);
				} // if $obj6 already loaded

				// Add the $obj1 (Gradeunit) to the collection in $obj6 (Local)
				$obj6->addGradeunit($obj1);

			} // if joined row is not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}

	/**
	 * Returns the TableMap related to this peer.
	 * This method is not needed for general use but a specific application could have a need.
	 * @return     TableMap
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function getTableMap()
	{
		return Propel::getDatabaseMap(self::DATABASE_NAME)->getTable(self::TABLE_NAME);
	}

	/**
	 * Add a TableMap instance to the database for this peer class.
	 */
	public static function buildTableMap()
	{
	  $dbMap = Propel::getDatabaseMap(BaseGradeunitPeer::DATABASE_NAME);
	  if (!$dbMap->hasTable(BaseGradeunitPeer::TABLE_NAME))
	  {
	    $dbMap->addTableObject(new GradeunitTableMap());
	  }
	}

	/**
	 * The class that the Peer will make instances of.
	 *
	 * If $withPrefix is true, the returned path
	 * uses a dot-path notation which is tranalted into a path
	 * relative to a location on the PHP include_path.
	 * (e.g. path.to.MyClass -> 'path/to/MyClass.php')
	 *
	 * @param      boolean  Whether or not to return the path wit hthe class name 
	 * @return     string path.to.ClassName
	 */
	public static function getOMClass($withPrefix = true)
	{
		return $withPrefix ? GradeunitPeer::CLASS_DEFAULT : GradeunitPeer::OM_CLASS;
	}

	/**
	 * Method perform an INSERT on the database, given a Gradeunit or Criteria object.
	 *
	 * @param      mixed $values Criteria or Gradeunit object containing data that is used to create the INSERT statement.
	 * @param      PropelPDO $con the PropelPDO connection to use
	 * @return     mixed The new primary key.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doInsert($values, PropelPDO $con = null)
	{
    // symfony_behaviors behavior
    foreach (sfMixer::getCallables('BaseGradeunitPeer:doInsert:pre') as $sf_hook)
    {
      if (false !== $sf_hook_retval = call_user_func($sf_hook, 'BaseGradeunitPeer', $values, $con))
      {
        return $sf_hook_retval;
      }
    }

		if ($con === null) {
			$con = Propel::getConnection(GradeunitPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}

		if ($values instanceof Criteria) {
			$criteria = clone $values; // rename for clarity
		} else {
			$criteria = $values->buildCriteria(); // build Criteria from Gradeunit object
		}

		if ($criteria->containsKey(GradeunitPeer::ID) && $criteria->keyContainsValue(GradeunitPeer::ID) ) {
			throw new PropelException('Cannot insert a value for auto-increment primary key ('.GradeunitPeer::ID.')');
		}


		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		try {
			// use transaction because $criteria could contain info
			// for more than one table (I guess, conceivably)
			$con->beginTransaction();
			$pk = BasePeer::doInsert($criteria, $con);
			$con->commit();
		} catch(PropelException $e) {
			$con->rollBack();
			throw $e;
		}

    // symfony_behaviors behavior
    foreach (sfMixer::getCallables('BaseGradeunitPeer:doInsert:post') as $sf_hook)
    {
      call_user_func($sf_hook, 'BaseGradeunitPeer', $values, $con, $pk);
    }

		return $pk;
	}

	/**
	 * Method perform an UPDATE on the database, given a Gradeunit or Criteria object.
	 *
	 * @param      mixed $values Criteria or Gradeunit object containing data that is used to create the UPDATE statement.
	 * @param      PropelPDO $con The connection to use (specify PropelPDO connection object to exert more control over transactions).
	 * @return     int The number of affected rows (if supported by underlying database driver).
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doUpdate($values, PropelPDO $con = null)
	{
    // symfony_behaviors behavior
    foreach (sfMixer::getCallables('BaseGradeunitPeer:doUpdate:pre') as $sf_hook)
    {
      if (false !== $sf_hook_retval = call_user_func($sf_hook, 'BaseGradeunitPeer', $values, $con))
      {
        return $sf_hook_retval;
      }
    }

		if ($con === null) {
			$con = Propel::getConnection(GradeunitPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}

		$selectCriteria = new Criteria(self::DATABASE_NAME);

		if ($values instanceof Criteria) {
			$criteria = clone $values; // rename for clarity

			$comparison = $criteria->getComparison(GradeunitPeer::ID);
			$selectCriteria->add(GradeunitPeer::ID, $criteria->remove(GradeunitPeer::ID), $comparison);

		} else { // $values is Gradeunit object
			$criteria = $values->buildCriteria(); // gets full criteria
			$selectCriteria = $values->buildPkeyCriteria(); // gets criteria w/ primary key(s)
		}

		// set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		$ret = BasePeer::doUpdate($selectCriteria, $criteria, $con);

    // symfony_behaviors behavior
    foreach (sfMixer::getCallables('BaseGradeunitPeer:doUpdate:post') as $sf_hook)
    {
      call_user_func($sf_hook, 'BaseGradeunitPeer', $values, $con, $ret);
    }

    return $ret;
	}

	/**
	 * Method to DELETE all rows from the GradeUnit table.
	 *
	 * @return     int The number of affected rows (if supported by underlying database driver).
	 */
	public static function doDeleteAll($con = null)
	{
		if ($con === null) {
			$con = Propel::getConnection(GradeunitPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		$affectedRows = 0; // initialize var to track total num of affected rows
		try {
			// use transaction because $criteria could contain info
			// for more than one table or we could emulating ON DELETE CASCADE, etc.
			$con->beginTransaction();
			$affectedRows += BasePeer::doDeleteAll(GradeunitPeer::TABLE_NAME, $con);
			// Because this db requires some delete cascade/set null emulation, we have to
			// clear the cached instance *after* the emulation has happened (since
			// instances get re-added by the select statement contained therein).
			GradeunitPeer::clearInstancePool();
			GradeunitPeer::clearRelatedInstancePool();
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Method perform a DELETE on the database, given a Gradeunit or Criteria object OR a primary key value.
	 *
	 * @param      mixed $values Criteria or Gradeunit object or primary key or array of primary keys
	 *              which is used to create the DELETE statement
	 * @param      PropelPDO $con the connection to use
	 * @return     int 	The number of affected rows (if supported by underlying database driver).  This includes CASCADE-related rows
	 *				if supported by native driver or if emulated using Propel.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	 public static function doDelete($values, PropelPDO $con = null)
	 {
		if ($con === null) {
			$con = Propel::getConnection(GradeunitPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}

		if ($values instanceof Criteria) {
			// invalidate the cache for all objects of this type, since we have no
			// way of knowing (without running a query) what objects should be invalidated
			// from the cache based on this Criteria.
			GradeunitPeer::clearInstancePool();
			// rename for clarity
			$criteria = clone $values;
		} elseif ($values instanceof Gradeunit) { // it's a model object
			// invalidate the cache for this single object
			GradeunitPeer::removeInstanceFromPool($values);
			// create criteria based on pk values
			$criteria = $values->buildPkeyCriteria();
		} else { // it's a primary key, or an array of pks
			$criteria = new Criteria(self::DATABASE_NAME);
			$criteria->add(GradeunitPeer::ID, (array) $values, Criteria::IN);
			// invalidate the cache for this object(s)
			foreach ((array) $values as $singleval) {
				GradeunitPeer::removeInstanceFromPool($singleval);
			}
		}

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		$affectedRows = 0; // initialize var to track total num of affected rows

		try {
			// use transaction because $criteria could contain info
			// for more than one table or we could emulating ON DELETE CASCADE, etc.
			$con->beginTransaction();
			
			$affectedRows += BasePeer::doDelete($criteria, $con);
			GradeunitPeer::clearRelatedInstancePool();
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Validates all modified columns of given Gradeunit object.
	 * If parameter $columns is either a single column name or an array of column names
	 * than only those columns are validated.
	 *
	 * NOTICE: This does not apply to primary or foreign keys for now.
	 *
	 * @param      Gradeunit $obj The object to validate.
	 * @param      mixed $cols Column name or array of column names.
	 *
	 * @return     mixed TRUE if all columns are valid or the error message of the first invalid column.
	 */
	public static function doValidate(Gradeunit $obj, $cols = null)
	{
		$columns = array();

		if ($cols) {
			$dbMap = Propel::getDatabaseMap(GradeunitPeer::DATABASE_NAME);
			$tableMap = $dbMap->getTable(GradeunitPeer::TABLE_NAME);

			if (! is_array($cols)) {
				$cols = array($cols);
			}

			foreach ($cols as $colName) {
				if ($tableMap->containsColumn($colName)) {
					$get = 'get' . $tableMap->getColumn($colName)->getPhpName();
					$columns[$colName] = $obj->$get();
				}
			}
		} else {

		}

		return BasePeer::doValidate(GradeunitPeer::DATABASE_NAME, GradeunitPeer::TABLE_NAME, $columns);
	}

	/**
	 * Retrieve a single object by pkey.
	 *
	 * @param      int $pk the primary key.
	 * @param      PropelPDO $con the connection to use
	 * @return     Gradeunit
	 */
	public static function retrieveByPK($pk, PropelPDO $con = null)
	{

		if (null !== ($obj = GradeunitPeer::getInstanceFromPool((string) $pk))) {
			return $obj;
		}

		if ($con === null) {
			$con = Propel::getConnection(GradeunitPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$criteria = new Criteria(GradeunitPeer::DATABASE_NAME);
		$criteria->add(GradeunitPeer::ID, $pk);

		$v = GradeunitPeer::doSelect($criteria, $con);

		return !empty($v) > 0 ? $v[0] : null;
	}

	/**
	 * Retrieve multiple objects by pkey.
	 *
	 * @param      array $pks List of primary keys
	 * @param      PropelPDO $con the connection to use
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function retrieveByPKs($pks, PropelPDO $con = null)
	{
		if ($con === null) {
			$con = Propel::getConnection(GradeunitPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$objs = null;
		if (empty($pks)) {
			$objs = array();
		} else {
			$criteria = new Criteria(GradeunitPeer::DATABASE_NAME);
			$criteria->add(GradeunitPeer::ID, $pks, Criteria::IN);
			$objs = GradeunitPeer::doSelect($criteria, $con);
		}
		return $objs;
	}

	// symfony behavior
	
	/**
	 * Returns an array of arrays that contain columns in each unique index.
	 *
	 * @return array
	 */
	static public function getUniqueColumnNames()
	{
	  return array();
	}

	// symfony_behaviors behavior
	
	/**
	 * Returns the name of the hook to call from inside the supplied method.
	 *
	 * @param string $method The calling method
	 *
	 * @return string A hook name for {@link sfMixer}
	 *
	 * @throws LogicException If the method name is not recognized
	 */
	static private function getMixerPreSelectHook($method)
	{
	  if (preg_match('/^do(Select|Count)(Join(All(Except)?)?|Stmt)?/', $method, $match))
	  {
	    return sprintf('BaseGradeunitPeer:%s:%1$s', 'Count' == $match[1] ? 'doCount' : $match[0]);
	  }
	
	  throw new LogicException(sprintf('Unrecognized function "%s"', $method));
	}

} // BaseGradeunitPeer

// This is the static code needed to register the TableMap for this table with the main Propel class.
//
BaseGradeunitPeer::buildTableMap();

